---
description: 修改代码后同步更新根目录 代码说明.md
globs: "**/*.{py,html,json,md}"
alwaysApply: true
---

# 代码说明文档同步

本项目根目录 `代码说明.md` 是**唯一权威的代码结构说明**（中文）。

## 何时更新

在完成以下任一变更后，**同一任务内**必须更新 `代码说明.md` 对应章节（勿单独提交过时的说明）：

- 新增/删除/重命名文件或目录
- 新增/变更 REST API 路由
- 变更 SQLite 表结构或 IntelRecord 字段
- 变更监测任务状态机、登录门禁、爬虫流程
- 变更 `config.json` 顶层配置键
- 修复影响用户故事或模块边界的逻辑

## 如何更新

1. 更新文档顶部的 **「文档版本」** 日期与 **「变更摘要」** 一行。
2. 修改 **目录树**、**模块说明**、**API 索引**、**数据流** 中受影响部分；未变章节不必重写。
3. 若发现代码与文档/规格不一致，**以代码为准修正文档**，并在变更摘要中注明；必要时同步 `docs/` 与 `openspec/specs/`。

## 禁止

- 不要创建第二份并行的架构说明（避免与 `代码说明.md` 冲突）。
- 不要在 `代码说明.md` 中写入 API Key、Cookie 等密钥明文。
